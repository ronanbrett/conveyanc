import { shallowMount } from '@vue/test-utils';
import PropertyCreatePage from '../PropertyCreatePage.vue';

test('PropertyCreatePage', () => {
  // render the component
  const wrapper = shallowMount(PropertyCreatePage);
});
