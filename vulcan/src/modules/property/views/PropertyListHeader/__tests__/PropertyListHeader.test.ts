import { shallowMount } from '@vue/test-utils';
import PropertyListHeader from '../PropertyListHeader.vue';

test('PropertyListHeader', () => {
  // render the component
  const wrapper = shallowMount(PropertyListHeader);
});
