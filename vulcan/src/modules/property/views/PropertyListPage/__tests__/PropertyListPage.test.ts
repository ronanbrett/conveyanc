import { shallowMount } from '@vue/test-utils';
import PropertyListPage from '../PropertyListPage.vue';

test('PropertyListPage', () => {
  // render the component
  const wrapper = shallowMount(PropertyListPage);
});
