import { shallowMount } from '@vue/test-utils';
import PropertyCreateTypeInput from '../PropertyCreateTypeInput.vue';

test('PropertyCreateTypeInput', () => {
  // render the component
  const wrapper = shallowMount(PropertyCreateTypeInput);
});
