import { shallowMount } from '@vue/test-utils';
import CardTransitionAnimation from '../CardTransitionAnimation.vue';

test('CardTransitionAnimation', () => {
  // render the component
  const wrapper = shallowMount(CardTransitionAnimation);
});
